environment: it
api:
  rest:
    client:
      bffssox:
        base-path: ${forge-it.wiremock.base-url:http://localhost:8080}/bffssox
      athssox:
        base-path: ${forge-it.wiremock.base-url:http://localhost:8080}/authsox
notification:
  messages:
    emailVerification:
      allowedChannels: [ email ]
      delivery:
        email:
          link:
            url: https://app.sitionix.com/auth/email/verify
